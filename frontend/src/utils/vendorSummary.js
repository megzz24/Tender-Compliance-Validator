/*
vendorSummary(vendorName, checkedReqs, results)
Computes met, partial, missing counts and
average confidence from the results data.
Returns { met, partial, missing, avgConf, score }.
Used by VendorCard and DashboardPage.
*/