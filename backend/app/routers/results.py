"""
GET /api/sessions/{id}/results
    Returns all vendor_results rows for the session.
    Each row contains the full results JSONB object.
    
GET /api/sessions/{id}/risks
    Returns all risk_reports rows for the session.
"""